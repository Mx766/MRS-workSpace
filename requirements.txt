# 文档翻译工具 — 依赖清单
# 安装: pip install -r requirements.txt

# 核心依赖
httpx>=0.27.0
pymupdf>=1.23.0
python-docx>=1.1.0
python-pptx>=0.6.0
cryptography>=42.0.0
openpyxl>=3.1.0

# GUI — tkinter 是 Python 内置模块，无需安装

# Windows 专用（Word COM OCR）
pywin32>=306; sys_platform == 'win32'

# 可选（命令行增强）
gradio>=4.0.0  # Web UI（可选）

# 开发依赖（仅开发时需要）
# pytest>=8.0.0
# pytest-cov>=5.0.0
